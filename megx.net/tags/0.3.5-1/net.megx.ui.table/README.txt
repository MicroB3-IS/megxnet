Extension for rendering common tables in all megx.net platform

Tables can be:
	1. Simple -> rendered directly in HTML and than wrapped by jquery datatable
	2. Server Side, no processing -> all data is returned from server at once, all prossessing (filtering, pagination) is done on client
	3. Server Side, with processing -> server returns specific pages requested by client


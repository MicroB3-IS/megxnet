<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<prerequisites>
		<maven>3.0.0</maven>
	</prerequisites>

	<groupId>net.megx</groupId>
	<artifactId>parent-microb3-is</artifactId>
	<packaging>pom</packaging>
	<version>1.0.0-SNAPSHOT</version>

	<repositories>
		<repository>
			<id>chon-repo</id>
			<layout>p2</layout>
			<url>http://p2.choncms.com/SNAPSHOT
		 </url>
		</repository>
	</repositories>


	<properties>
		<!-- all maven properties get the mvn prefix -->
		<!-- just dump defaults: need to be overwritten in profiles etc. -->
		<mvn.tomcat.home>${user.home}/tomcat</mvn.tomcat.home>
		<mvn.tomcat.port>8080</mvn.tomcat.port>
		<mvn.work.dir.name>megx.net-work.dir</mvn.work.dir.name>
		<!-- not sure if should stay here -->
		<mvn.work.dir.path>${user.home}/${mvn.work.dir.name}</mvn.work.dir.path>
		<tycho.version>0.14.0</tycho.version>
		<tycho.groupid>org.eclipse.tycho</tycho.groupid>
	</properties>


	<modules>
		<!-- build megx.net core bundle -->
		<module>net.megx.chon.core</module>
		<!-- build megx.net web services related: TODO refactor -> delete -->
		<module>net.megx.chon.services</module>
		<!-- build megx.net web services related: TODO refactor -> delete -->
		<module>net.megx.chon.services.ui</module>
		<!-- build megx.net velocity templates -->
		<module>net.megx.chon.template</module>

		<!-- where and what is chon and get all chon bundles -->
		<module>target-definition</module>

		<!-- what bundles will be included in the final build -->
		<module>net.megx.product</module>
		<!-- does not need to be build always: TODO configure the setup.proporteis 
			according to build -->
		<module>megx.net-web</module>
	</modules>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>megx.net-web</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>net.megx.chon.core</artifactId>
				<version>1.0.0-SNAPSHOT</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>net.megx.chon.services</artifactId>
				<version>1.0.0-SNAPSHOT</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>net.megx.chon.services.ui</artifactId>
				<version>1.0.0-SNAPSHOT</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>net.megx.chon.template</artifactId>
				<version>1.0.0-SNAPSHOT</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>web-content-repository</artifactId>
				<version>1.0-SNAPSHOT</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>net.megx.product</artifactId>
				<version>${project.version}</version>
			</dependency>

		</dependencies>
	</dependencyManagement>

	<profiles>
		<profile>
			<id>renzo-linux</id>
			<activation>
				<property>
					<name>user.name</name>
					<value>rkottman</value>
				</property>
			</activation>
			<properties>
				<mvn.tomcat.home>${user.home}/progs/tomcat</mvn.tomcat.home>
				<mvn.tomcat.port>8199</mvn.tomcat.port>
				<mvn.work.dir.path>/tmp/${mvn.work.dir.name}</mvn.work.dir.path>
			</properties>
		</profile>
		<profile>
			<id>renzo-laptop</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
				<property>
					<name>user.name</name>
					<value>rkottman</value>
				</property>
			</activation>
			<properties>
				<mvn.work.dir.path>E:\dev\${mvn.work.dir.name}</mvn.work.dir.path>
				<mvn.tomcat.home>E:\dev\apache-tomcat-6.0.35-windows-x64</mvn.tomcat.home>
				<mvn.tomcat.port>8080</mvn.tomcat.port>
			</properties>
		</profile>
		<profile>
			<id>julia-laptop</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
				<property>
					<name>user.name</name>
					<value>jschnetz</value>
				</property>
			</activation>
			<properties>
				<mvn.work.dir.path>D:\${mvn.work.dir.name}</mvn.work.dir.path>
				<mvn.tomcat.home>D:\Programs\apache-tomcat-7.0.27</mvn.tomcat.home>
				<mvn.tomcat.port>8080</mvn.tomcat.port>
			</properties>
		</profile>
	</profiles>



	<build>
		<!-- configuring for inheritance -->
		<!-- Plugin Management contains plugin elements in much the same way, except 
			that rather than configuring plugin information for this particular project 
			build, it is intended to configure project builds that inherit from this 
			one. However, this only configures plugins that are actually referenced within 
			the plugins element in the children. The children have every right to override 
			pluginManagement definitions. https://maven.apache.org/pom.html#Plugin_Management -->
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>${tycho.groupid}</groupId>
					<artifactId>target-platform-configuration</artifactId>
					<version>${tycho.version}</version>
				</plugin>

				<plugin>
					<groupId>${tycho.groupid}</groupId>
					<artifactId>tycho-maven-plugin</artifactId>
					<version>${tycho.version}</version>
					<extensions>true</extensions>
				</plugin>
			</plugins>

		</pluginManagement>

		<plugins>

			<plugin>
				<artifactId>maven-clean-plugin</artifactId>
				<version>2.4.1</version>
				<configuration>
					<filesets>
						<fileset>
							<directory>${mvn.work.dir.path}</directory>
						</fileset>
					</filesets>
				</configuration>
			</plugin>

			<plugin>
				<groupId>${tycho.groupid}</groupId>
				<artifactId>target-platform-configuration</artifactId>
				<version>${tycho.version}</version>
				<configuration>
					<resolver>p2</resolver>
					<target>
						<artifact>
							<groupId>net.megx</groupId>
							<artifactId>target-definition</artifactId>
							<version>1.0.0-SNAPSHOT</version>
							<classifier>chon</classifier>
						</artifact>
					</target>
					<ignoreTychoRepositories>true</ignoreTychoRepositories>
				</configuration>
			</plugin>

			<plugin>
				<groupId>${tycho.groupid}</groupId>
				<artifactId>tycho-maven-plugin</artifactId>
				<version>${tycho.version}</version>
			</plugin>


		</plugins>
	</build>
	<organization>
		<name>Micro B3</name>
		<url>http://www.microb3.eu</url>
	</organization>
	<inceptionYear>2012</inceptionYear>
	<developers>
		<!-- in alphabetical order of last Name -->
		<developer>
			<name>Sebastian Gross</name>
		</developer>
		<developer>
			<name>Jovica Jonoski</name>
		</developer>
		<developer>
			<name>Renzo Kottmann</name>
		</developer>
		<developer>
			<name>Pavle Jonoski</name>
		</developer>
		<developer>
			<name>Michael Schneider</name>
		</developer>
		<developer>
			<name>Julia Schnetzer</name>
		</developer>
	</developers>
	<licenses>
		<license>
			<name>Apache 2 License</name>
		</license>
	</licenses>
</project>

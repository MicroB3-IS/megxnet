<settings xmlns="http://maven.apache.org/SETTINGS/1.1.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.1.0
                      http://maven.apache.org/xsd/settings-1.1.0.xsd">

    <!-- This is an example Maven settings.xml file.
        It contains specific Chon and megx.net build settings specific for
        local compute environments.
        Please copy whole file or contents for your specific environments
        and adjust configuration accordingly
    -->
    <mirrors>
        <!-- mirror | Specifies a repository mirror site to use instead of
            a given
            repository. The repository that | this mirror serves has an ID
            that matches
            the mirrorOf element of this mirror. IDs are used | for inheritance
            and direct
            lookup purposes, and must be unique across the set of mirrors.
            | -->
        <mirror>
            <id>local-indigo</id>
            <mirrorOf>indigo</mirrorOf>
            <name>Local Mirror of Eclipse Indigo p2 repository.</name>
            <url>file:///E:/dev/eclipse-repo-mirror</url>
            <layout>p2</layout>
            <!-- this is the trick to let tycho take the mirror: see e.g.
                http://www.iulidragos.org/?p=207 -->
            <mirrorOfLayouts>p2</mirrorOfLayouts>
        </mirror>
    </mirrors>


    <profiles>
        <profile>
            <id>renzo-laptop</id>
            <activation>
                <os>
                    <family>Windows</family>
                </os>
                <property>
                    <name>user.name</name>
                    <value>rkottman</value>
                </property>
            </activation>
            <properties>
                <mvn.work.dir.name>megx-dev-dir</mvn.work.dir.name>
                <mvn.work.dir.path>E:\dev\${mvn.work.dir.name}</mvn.work.dir.path>
                <mvn.tomcat.home>E:\dev\apache-tomcat-6.0.35-windows-x64</mvn.tomcat.home>
                <mvn.tomcat.port>8080</mvn.tomcat.port>
            </properties>
        </profile>
    </profiles>
</settings>